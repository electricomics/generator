{
  "title": "{{jsesc title}}",
  "summary": "{{jsesc summary}}",
  "creators": [
    {{#creators}} {"name": "{{jsesc name}}", "role": "{{jsesc role}}" } {{#unless @last}},{{/unless}} {{/creators}}
  ],
  "slug": "electricomics-comic",
  "version": {{version}},
  "publish_date": "{{dateString}}",
  "config" : {
    "orientation":"{{orientation}}",
    "buttons": [
      {
        "type": "sound",
        "action": "elcx:toggleAudio"
      },
      {
        "type": "hint",
        "action": "elcx:hint"
      }
    ],
    "features": ["tap"]
  }
}